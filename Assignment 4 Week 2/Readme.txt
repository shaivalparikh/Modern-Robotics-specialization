%Readme

Please view the 'Generalcode' first, then 'starightlinedistance', then 'randomsample', then 'nodescheck' and then at last 'validedges'. 'Astar' code is optional
as it is the same code I wrote for the first assignment. It takes in the nodes and edges data and gives the most optimal path.